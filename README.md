Create a Project Folder: Open a terminal and run mkdir langchain-app && cd langchain-app.
Open in VS Code: Type code . in your terminal to open this folder in VS Code
Create a Virtual Environment (Recommended): This keeps dependencies isolated.Windows/macOS/Linux: python -m venv venv
Activate the Environment:Windows: venv\Scripts\activate
pip install langchain langchain-openai python-dotenv
Create a file named .env and add your key
Install VS Code Extensions: Go to the Extensions view (Ctrl+Shift+X) and install the Python extension by Microsoft.
Create a Python File: Create a file named app.py.
sample prompt
how to write and execute langchain code in vss. what is installation and set-up that i need to do and execute code
